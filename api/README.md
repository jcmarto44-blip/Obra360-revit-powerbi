   # api
   
   Backend en Node.js (desplegado en Render).
   
   Recibe los datos del modelo de Revit (geometría + parámetros),
   los guarda en Supabase, y expone los datos hacia Power BI / el
   visual Obra360Pulse.
